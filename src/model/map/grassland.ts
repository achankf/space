
/*
export function grasslandMap(
    width: number,
    height: number,
): IMap {
    const dimension = width * height;
    const terrains = new Array<Terrain>(dimension).fill(Terrain.Grassland);
    const map: IMap = {
        height,
        terrains,
        width,
    };
    return map;
}
*/
